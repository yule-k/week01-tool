# Temperature Advisor
Converts a temperature between Celsius and Fahrenheit and reports
whether the result is in a comfortable range.
## Setup
python -m venv .venv
source .venv/bin/activate # Windows: .venv\Scripts\activate
pip install -r requirements.txt
## Run
python app.py